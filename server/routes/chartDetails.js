const express = require('express')
const router = express.Router()
const preEvent = require('../models/preEvent')
const postEvent = require('../models/postEvent')


router.get('/getBarDetails', (req, res) => {
    let count = {
        
    }
})